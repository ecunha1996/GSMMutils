


from ExpAlgae import io
from ExpAlgae import utils
from ExpAlgae import experimental
from ExpAlgae import model

from os.path import join, dirname, abspath
DATA_PATH = abspath(join(dirname(__file__), '../../data'))
DFBALAB_PATH = abspath(join(dirname(__file__), '../matlab/DFBAlab'))
